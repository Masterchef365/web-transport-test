[package]
name = "reverse-common"
version = "0.1.0"
edition = "2021"

[dependencies]
framework = { workspace = true }
serde = { workspace = true, features = ["derive"] }
tarpc = { workspace = true, features = ["serde1"] }
